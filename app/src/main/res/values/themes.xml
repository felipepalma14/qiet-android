<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal do app -->
    <style name="Theme.Qiet" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Cores primárias -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Cores secundárias -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Cor da status bar -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Personalizações adicionais podem ser adicionadas aqui -->
    </style>

    <!-- Tema sem ActionBar, usado na SplashActivity -->
    <style name="Theme.Qiet.NoActionBar" parent="Theme.Qiet">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
